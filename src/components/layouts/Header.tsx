function Header() {
  return (
    <header className="fixed top-0 z-50 w-full border-b border-2 bg-background">
      <nav className="containter mx-auto flex items-center h-16">
        Navigation
      </nav>
    </header>
  );
}

export default Header;
